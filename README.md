# menu-copy-as-markdown
A floating web view inside BetterTouchTool that allows you to:  - Copy the currently selected text on web pages in CloudCode and in applications to Markdown   - Copy the selected text in the same locations, but with the option to wrap that text in a Markdown block quote
